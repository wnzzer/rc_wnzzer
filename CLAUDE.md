# 本仓库的协作工作流（给 AI 助手和人类协作者的常驻约定）

本项目是一份「AI Coding 作业」。作业的评分重点**不是功能是否齐全**，而是：
问题拆解、边界界定、复杂度管理，以及**对 AI 输出的判断、修正与取舍能力**。

因此本仓库有一条硬性纪律：**过程本身就是交付物**。

---

## 1. 三本账，必须同步维护

| 文件 | 性质 | 什么时候写 |
|---|---|---|
| `docs/ai-session-log.md` | 工作流水账 | 每完成一个**有产出的轮次**，追加一条 |
| `docs/decisions.md` | 取舍与否定记录 | 每做出一个**有代价的选择**，追加一条 |
| `AI_USAGE.md` | 作业交付物 | 收尾时从上面两本账**汇总生成**，不手写原始信息 |

> 前两本是原始账本，第三本是对外汇报。**不允许出现只在 AI_USAGE.md 里、
> 在账本里查无实据的说法** —— 那是编故事，不是工程记录。

### 1.1 `ai-session-log.md` 的条目格式

```markdown
### R{n} · {一句话主题}
- **人类输入**：（意图，不是逐字原文）
- **AI 产出**：（AI 给了什么：方案 / 代码 / 反问）
- **结论**：（最终采纳了什么 / 改成了什么）
- **落到文件**：`path/a`、`path/b`
- **关联决策**：D-00x（若有）
```

颗粒度规则：**记工作流，不记逐字稿**。一轮对话若只是确认、没有产出，合并进下一轮。

### 1.2 `decisions.md` 的条目格式

```markdown
### D-00n · {决策名}
- **状态**：已采纳 / 已否决 / 待定 / 已被 D-00m 取代
- **背景**：为什么这里需要做选择
- **选项**：A / B / C，各自代价
- **决定**：选了什么
- **理由**：为什么。**必须写出放弃了什么**，只写好处的不算决策记录
- **反悔成本**：如果这个决定错了，改回来要付出什么
```

**否决项和采纳项同等重要。** AI 建议但没被采纳的方案，必须以「已否决」状态入账，
写清否决理由 —— 这一条直接对应作业的必答题「哪些设计是过度的」。

---

## 2. 脱敏纪律（不可协商）

三本账会随仓库公开到 GitHub。写入任何文件前，按下表替换：

| 真实信息 | 写成 |
|---|---|
| 真实姓名 | 「作者」 |
| 公司名 / 产品名 / 组织名 | 「某公司」「内部业务系统」 |
| 公司邮箱、个人邮箱 | 省略，或 `user@example.com` |
| 内网域名、内网 IP、跳板机 | `internal.example.com` / `203.0.113.10`（RFC 5737 文档保留段） |
| 生产/测试环境的服务名、库名、表名 | 泛化为「某内部服务」 |
| 任何 token / key / 密码 / open_id | 删除，不做「部分打码」 |
| 本机绝对路径 `/Users/<name>/...` | `~/rc_wnzzer/...` |
| 与本作业无关的其他项目上下文 | 直接不写 |

**判断原则**：一条信息如果对「说明工程判断」没有贡献，但能定位到真实的人或系统，
就删掉。脱敏优先级高于记录完整性。

提交前必须跑：

```bash
./scripts/redact-check.sh
```

该脚本是**兜底**不是保险 —— 它只能查已知模式，写入时的自觉才是第一道防线。

---

## 3. 工程约束（本项目的不变量）

这些是项目立项时定下的硬约束，改动需要新开一条 ADR 并说明理由：

1. **零第三方依赖**。只用语言标准库。构建产物是单一静态二进制。
2. **单进程常驻后台服务**。不引入 MQ、外部数据库、调度框架、容器编排。
3. **必须做到**：指数退避（含 jitter）、幂等重试、崩溃不丢已确认任务。
4. **对称密钥鉴权**：请求签名，不用明文长期 token。
5. **MVP 优先**。任何「以后可能需要」的功能，写进演进路线，不写进 v1 代码。

第 5 条的执行方式：当你想加一个功能时，先问「不加它，v1 会坏在哪」。
答不上来，就只记进 `decisions.md` 的否决区。

---

## 4. 工作循环

```
需求/问题
   ↓
[人类] 定方向、给约束、拍板取舍
   ↓
[AI] 出方案 + 主动标出「这一步我可能过度设计了」
   ↓
[人类] 采纳 / 修正 / 否决      ←── 否决也要留痕
   ↓
落代码 / 落文档
   ↓
追加 session-log（做了什么） + decisions（为什么这么选）
   ↓
scripts/redact-check.sh → commit
```

**卡点规则**：遇到会实质改变工作方向的歧义（语言选型、边界划分、语义定义），
AI 停下来提问，不要自行假设后一路做下去。已经明确的约束不要反复确认。
