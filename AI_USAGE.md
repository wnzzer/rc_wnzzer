# AI 使用说明

> 本文是作业要求的第三份交付物。它是**汇总产物**，不是原始记录 ——
> 每一条都能在 [`docs/ai-session-log.md`](docs/ai-session-log.md)（工作流水账）
> 和 [`docs/decisions.md`](docs/decisions.md)（40 条取舍记录）里找到出处。
>
> 仓库里写死了一条纪律（见 [`CLAUDE.md`](CLAUDE.md) §1）：
> **不允许出现只在本文里、在原始账里查无实据的说法。** 否则这就是一篇作文，不是工程记录。

---

## 0. 一句话总结

AI 在本项目里最有价值的用法**不是让它生成方案**，而是
**让它把自己的默认建议全部摆到台面上，再逐条检验哪些配不上它们的复杂度**。

AI 对这类需求的条件反射是给「完备」的架构：消息队列、熔断器、供应商适配层、
多实例 HA、插件化存储。这些建议本身不算错，只是**默认了一个本项目并不处于的规模**。
工程价值恰恰在于把它们一条条挡回去，并留下挡回去的理由 ——
最终 40 条决策里有 **29 条是否决**。

---

## 1. AI 在哪些关键地方提供了帮助

### 1.1 把三个业务例子归纳成一个结构性问题

需求给的是三个并列场景（广告回传 / CRM 状态变更 / 库存变更）。AI 指出它们共享同一个
结构性问题：

> **一次关键业务事件的提交成功，被绑定在了一个不受控的第三方的可用性上。**

并进一步指出「业务方不关心返回值」是整个设计**最关键的输入** ——
它一次性排除了回调、结果队列、响应透传等一整类设计（对应边界 B3、决策 D-026）。
这个归纳直接决定了后面所有边界的划法。

### 1.2 在语言选型上给出带代价的选项，而不是推销推荐项

零依赖约束下，「语言」和「持久化机制」必须联合选择。AI 给了三个方案，
并**明确说明了它不推荐的那个方案的优势**：Python 的 `sqlite3` 属于标准库，
仍算零依赖，却能白拿 WAL 模式的事务与崩溃安全。

这是一个真实存在的强论点。最终选 Go + 手写 WAL（D-002），是**接受了
「自研存储需自证正确性」这个代价**去换设计深度 —— 而不是因为没看到另一条路。
兜底方案也一并写进了决策记录：若自研存储出现无法收敛的正确性问题，
可换纯 Go 的 SQLite 实现，代价是破坏零依赖约束。

### 1.3 非对称 fsync 策略的推导（本设计最核心的一处）

AI 提出对四类 WAL 记录**分别追问「丢了会怎样」**，而不是统一处理：

| 记录 | 丢失后果 | 违反核心承诺？ |
|---|---|---|
| `enq` | 任务彻底消失，业务方以为发了、实际没发，且无人知晓 | **是** |
| `att` | 重试次数偏小，多试几次 | 否 |
| `ok` | 已成功的任务被重投一次 | 否 |
| `dead` | 死信回队，再走一遍失败流程 | 否 |

结论：**只有 `enq` 需要同步 fsync**，其余三类的丢失后果**全部已被 at-least-once
语义本身吸收**。而 `ok` 恰好是数量上最多的记录 —— 对它 fsync 付出最大成本却换不到
任何正确性（D-005）。

这个推导把持久化成本精确花在了唯一需要它的地方，是全项目最有价值的一处设计。

### 1.4 区分「入口幂等」与「出口幂等」

AI 指出这一对概念的混淆是这类系统最常见的设计事故：

- **入口幂等**（去重*提交*）：notifyd 强保证，靠必填的 `idempotency_key`；
- **出口幂等**（去重*投递*）：**供应商的责任**，notifyd 只能通过恒定不变的
  `X-Notify-Id` 协助。

分清这两者之后，「exactly-once 做不到」就不再是一句托词，而是一个有明确责任划分的
工程结论（D-003、边界 B1）。

### 1.5 两处安全设计的具体攻击场景

AI 没有泛泛地说「要注意安全」，而是给出了具体的绕过路径：

- **签名必须覆盖 body 哈希**：本服务的请求体里装着**目标 URL**。只签时间戳的话，
  任何能改写请求体的中间人都能把一条「通知 CRM」改成「把用户数据 POST 到攻击者
  服务器」。（验收用例 `TestA6b_SignatureCoversBody` 把这个场景钉住了）
- **SSRF 校验必须钩在 Dial 层**：提交时解析 DNS 再放行存在 rebinding 的 TOCTOU 窗口；
  而且若跟随 3xx 重定向，一个 302 就能绕过校验。所以
  `net.Dialer.Control` + 禁止重定向，两者缺一不可（D-015、D-010）。

### 1.6 承担了绝大部分实现与测试的编写

约 2500 行 Go（不含测试）+ 完整的 A1-A10 验收测试套件。**在写完 WAL 后立刻补测试**，
而不是留到最后 —— 这个节奏让崩溃恢复、尾部截断修复这些难自证的逻辑在最早期就被钉住。

---

## 2. AI 给出过哪些我没有采纳的建议

完整清单见 [`docs/decisions.md`](docs/decisions.md) 的 29 条 ❌ 记录。
下面是最典型的六条，以及**在什么信号出现时我会改变主意**。

### 2.1 ❌ 引入消息队列（Kafka / RabbitMQ / Redis Streams）—— D-018

这几乎是这类需求的默认答案。我的检验方法是**逐条对照 MQ 的核心能力**：

| MQ 提供什么 | 本需求用得上吗 |
|---|---|
| 分区有序 | ❌ 三个业务场景没有一个需要顺序（B2） |
| 消费者组水平扩展 | ❌ 单实例足够（D-021） |
| 日志重放 | ❌ 不需要重放历史流量 |
| 跨系统解耦 | ❌ 解耦本来就是本服务自身提供的 |

**一条都用不上。** 引入它等于把「一个本地文件的复杂度」换成「一个集群的复杂度」：
运维、容量规划、版本升级、监控，外加一个新的 SPOF。

**不使用时的替代方案**：本地 append-only WAL（spec §7）。在单机可承载的量级下，
它提供了 MQ 在本场景中被实际用到的全部能力。

**什么时候改变主意**：出现顺序性需求、跨区域部署或流量重放需求时（演进 Stage 4）——
那时上面四项能力会同时用上，MQ 才配得上它的运维成本。

### 2.2 ❌ 熔断器 —— D-014

AI 建议给每个目标 host 加 closed/open/half-open 三态熔断器。

不采纳的理由是**收益已被覆盖**：per-host 并发上限（D-013）防止资源被单个坏目标占满，
full jitter 退避（D-011）防止恢复时的惊群 —— 熔断器的主要价值这两者已经拿到了。
而它额外带来一个三态机、探测策略，以及一组**没有先验数据就调不对**的阈值
（连续失败多少次？冷却多久？半开放多少流量？）。**误熔断比不熔断更危险**，
因为它会直接中断正常投递。

注意这里的判断和 D-013 是**相反**方向的：per-host 上限我留了，熔断器我砍了。
区别在于「不加它，v1 会坏在哪」这个问题 —— 前者有明确答案（一个 RT 30s 的供应商
占满全部 worker，其余供应商一起饿死），后者没有。

### 2.3 ❌ 供应商适配层 / payload 模板引擎 —— D-020

AI 建议内置供应商注册表，支持 OAuth token 自动刷新、各家签名算法、payload 字段映射模板。

这是**无界**需求：每接一家供应商就改一次 notifyd，最终长成谁也不敢动的适配器地狱，
且发布节奏被最慢的那家供应商绑架。更根本的是，**只有业务系统知道该往 CRM 传什么状态** ——
适配逻辑天然属于业务侧知识。

notifyd 的定位是「字节忠实的管道」，**保持无知是它的特性而非缺陷**。

### 2.4 ❌ 可插拔存储插件框架 —— D-023

AI 建议在 `Store` 接口之上加实现注册表，通过 `storage.driver=file|mysql|redis` 动态选择。

v1 只有一个实现。**为一个实现建注册表是典型的推测性通用化。**

值得注意的是我**保留了 `Store` 接口本身**（D-022）—— 这是全项目唯一一处为未来留的口子。
允许它存在的条件有两个且缺一不可：(1) 几乎零成本，本来就要分层；
(2) 不留的代价很高，否则文件 IO 会渗透进调度逻辑，日后换存储等于重写。
我给自己定的越界判据是：**如果一个抽象需要「注册机制 / 配置驱动的实现选择 /
生命周期钩子」，它就越界了。**

### 2.5 ❌ WAL 记录加 CRC 校验 —— D-007

append-only + 单写者模式下，崩溃能造成的**唯一**损坏形态是尾部截断，
它已被「JSON 解析失败 → 截断到最后一个完好偏移」可靠覆盖。
CRC 针对的是磁盘位翻转 —— 那是文件系统与硬件的职责（ZFS/btrfs 校验、ECC），
在应用层加一层弱校验属于**职责错配**，还会引出「中间坏了但后面是好的，要不要跳过续读」
这种没有正确答案的分支。

代价（真发生中段损坏会丢失其后记录）已列入 spec §11 自曝清单，而不是留着不说。

### 2.6 ❌ 其余被砍掉的

多实例 HA + 分布式锁（D-021）、外部数据库 + 调度框架（D-019）、
gRPC/protobuf 接口（D-025）、metrics SDK（D-024，改为手写约 100 行 Prometheus 文本）、
任务体静态加密 / KMS（D-028，属安全剧场）、死信管理 Web UI（D-029）、
响应体回传 / 结果回调（D-026）、顺序保证（D-027）、细粒度分片锁（D-016）。

还有一条方法论层面的：脱敏检查脚本遇到测试文件误报时，
**AI 的第一反应是排除 `*_test.go`，被我否决**（D-038）—— 那是在最容易疏忽的地方
关掉检查。改为行内 `redact-ok: <理由>` 标记，理由必填。

---

## 3. 哪些关键决策是我自己做出的，以及原因

### 3.1 零第三方依赖 + 单常驻进程（立项约束，D-001）

这是我在看到需求之后、让 AI 出方案之前就定下的约束。

理由：这个系统的真实复杂度**不在「接请求」也不在「发请求」**，而在
**崩溃不丢 + 退避正确 + 失败分类**。引入框架不会让这三件事变简单，只会把它们藏进
一堆配置里。反过来，零依赖**强迫每一行复杂度都被显式写出来、被显式论证**。

这条约束同时起到了「防止 AI 过度设计」的作用：任何「引入 X 就能解决」的建议，
在这条约束下都必须先回答「不引入时替代方案是什么、代价是什么」。

### 3.2 Go + 手写 WAL，而不是 Python + stdlib sqlite3（D-002）

AI 把两条路的代价都摆清楚了，选择是我做的。

选 Go 的理由不是「Python 不行」——`sqlite3` 属标准库、崩溃安全白拿，这是真优势。
而是：**手写 WAL 里的非对称 fsync 策略、尾部截断修复、rename 原子压缩，
正是最能体现工程判断的部分**，用 SQLite 会把这部分思考整个外包掉。

我为此接受的代价是「正确性需自证」。这笔债在 `test/acceptance/`（A1-A10，
含真实 `kill -9` 恢复测试）和 `internal/store/wal_test.go`（含三种尾部损坏形态）里还掉了。

### 3.3 投递语义定为 at-least-once，并写进对外契约（D-003）

不选 exactly-once 不是能力问题，是**诚实问题**。一次 POST 超时后，
「对方没收到」和「对方收到了但响应丢了」这两种世界状态在本服务侧**不可区分**，
没有任何本地机制能消除该歧义。声称 exactly-once 要么依赖对端配合（那就不是我们的保证），
要么是在撒谎。

所以：选 at-least-once，把重复的可能性**明确暴露**给调用方和供应商，
并用恒定的 `X-Notify-Id` 尽到我们能尽的责任。

### 3.4 边界划分（spec §2.2 的 B1-B10）

哪些不做、为什么不做、谁来做 —— 这一节是我逐条定的，也是我刻意写得比
「解决什么」更详细的一节。**边界是靠说清楚不做什么来定义的。**

其中最容易被挑战的两条我心里有数：B4（不做供应商适配）会被问「那你管什么」，
B5（v1 不做多实例）会被问「单点怎么办」。两条的完整论证分别在 D-020 和 D-021，
并都绑定了明确的演进触发信号。

### 3.5 会话记录采用结构化流水账而非逐字全文（D-030）

交付物需要的是**可复核的工程决策链**，不是聊天记录。逐字全文噪声大、脱敏风险高
（每一轮都可能夹带无关上下文），而且读者无法从中快速看出「哪里做了判断」。

作为补偿，我给流水账定了一条硬规则：**每条记录都必须能追溯到具体的文件改动** ——
保证它不是事后编的。

### 3.6 全量实现 + 全量验收测试（R7）

AI 给的选项里有「只做核心路径」。我选全量，因为**文档写了却不实现，
整份设计就显得像空谈** —— 而这份作业考的正是工程判断能否落地。

---

## 4. AI 出错、以及被测试反向修正的记录

这一节存在的理由：**只讲 AI 帮了什么忙，等于没讲 AI 怎么用。**
下面每一条都是 AI（包括它自查）产出的东西被推翻或修正的实例。

### 4.1 spec 里的默认参数自相矛盾（交付前自查发现）

初稿写 `base=1s, cap=1h, max_attempts=12`。交付前**没有通读、而是算了一遍**：

```
max_attempts=12  →  最坏 0.6h   期望 0.3h
max_attempts=24  →  最坏 12.1h  期望 6.1h
```

12 次只覆盖**期望 18 分钟**的宕机。而且 `cap=1h` 在 12 次下**从未被触发**
（第 12 次的窗口才 2048s），`deadline=24h` 更不可能先到 ——
**三个参数里有两个是死参数**。而 spec §1.2 的核心承诺白纸黑字写着「吸收小时级宕机」。

改为 24，并把「为什么两个放弃条件并存」整段重写，如实说明 deadline 是防御性兜底
而非主控（D-012）。

**那组参数每一个都有名字、看起来都合理，读十遍也读不出问题，算一遍就露馅。**

### 4.2 压缩触发条件量错了东西（写测试时发现，D-032）

初稿：`终态记录占比 > 50%` 时压缩。

写验收用例 A9 时发现这**量错了东西**：压缩的收益来自折叠同一任务的多条 `att` 记录
（重试 100 次 = 100 条 `att`，压缩后只剩 1 条），而这类记录对「终态占比」的贡献是 **0**。
实测 25 个任务累计 500+ 次重试，终态占比只有 3.4%，**压缩永远不会触发** ——
恰好漏掉了最该压缩的那种情况，而 journal 在持续膨胀。

改为直接比较「快照所需记录数 / 磁盘实际记录数」。这个比值**就是压缩收益本身**。
实测效果：532 次重试后 journal 稳定在 17KB。

**教训：一个指标「看起来相关」不等于它度量的是你要的东西。**

### 4.3 压缩把供应商凭据留在磁盘上 7 天（查体积问题时顺带发现，D-033）

排查压缩收益不足时发现：成功任务的墓碑保留了**完整 Target**，包括 `Headers` ——
而那里面装着供应商的 `Authorization` 凭据。一个已经投递成功的任务，
没有任何理由把别人的 token 在磁盘上再留 7 天。

**这本来是个体积问题，查下去发现它同时是个凭据留存问题。**
改为成功任务只留瘦墓碑（清掉 Headers/Body，保留 URL/Method 供排障）；
死信保留完整 Target（`/retry` 需要），代价写进 spec §11。

### 4.4 同一策略两处实现，其中一处漏了开关（端到端测试发现，D-034）

SSRF 防护有两处实现，`NOTIFY_ALLOW_PRIVATE_HOSTS` 只接到了 Dial 层，
提交预检**无条件**拒绝私网地址 —— 开关形同虚设，而症状（提交被 400 拒绝）
指向的方向完全错误。

第一次跑验收测试时 **12 个用例挂了 11 个**。单测各自都能过，只有端到端测试抓得到。

### 4.5 `-race` 跑了，却没检测到主体代码（D-039）

验收测试启动的是**独立进程**。对测试二进制开 `-race`，只能检测测试脚手架自己
（当时确实抓到脚手架里一个 `bytes.Buffer` 竞态），但**完全检测不到 notifyd 内部的并发** ——
而那才是真正需要检测的地方：32 个 worker、共享队列、后台刷盘协程、per-host 信号量。

**一个跑了 `-race` 却没覆盖主体代码的测试，给的是虚假的安全感，比不跑更危险，
因为它会让人停止怀疑。** 改为 race 模式下给被测二进制也加 `-race`。

### 4.6 示例脚本第一次被执行就露馅（D-040）

`scripts/notify-submit.sh` 里 `${3:-{"event":"demo"}}` 的默认值中那个 `}`
会**提前终止参数展开**，导致 payload 尾部多出一个 `}`。签名照样算得出来、
请求也发得出去，只是供应商收到的是坏 JSON。

这种 bug 读代码基本发现不了。它是被「真的去执行示例脚本」的验收用例抓到的。

### 4.7 借用错误哨兵导致误导性的死信原因（自查发现，D-035）

实现时为图省事，用 `ErrBlockedTarget` 去包装「body 无法解码」，能跑通 ——
但死信原因会显示成 `blocked_target`，把排障的人引向网段拦截，而真正的问题是
payload 编码写错了。

**一个会把人引到错误方向的错误信息，比没有错误信息更糟。** 拆成三个独立哨兵。

### 4.8 脱敏脚本自身静默失效（D-031）

脚本首次运行就抓到自己（`CLAUDE.md` 里一个私网段占位符）。排查那次误报时发现
脚本**自身有 bug**：邮箱规则误用了 `grep -E` 不支持的 PCRE 零宽断言，
失败被 `|| true` 静默吞掉 —— 结果该规则**永远不报警**。

这是最危险的一类缺陷：**一个看起来在工作、实际什么也没查的安全检查。**
修复后补了自测（4 条真阳性 + 2 条合法占位符）。后来写 `decisions.md` 时它又抓到我一次。

---

## 5. 这个项目里我认为最有价值的一条方法

**让 AI 把它自己的默认建议全部摆出来，再逐条问「不加它，v1 会坏在哪」。**

答得上来的留下（per-host 并发上限：一个 RT 30s 的供应商会占满全部 worker，
其余供应商一起饿死 —— 这是典型生产事故，修复成本 20 行，留）。

答不上来的进否决区，并写清「什么信号出现时重新引入」（熔断器：退避 + per-host 上限
已覆盖它 80% 的收益，阈值又没有先验数据可调 —— 砍，留到演进 Stage 3）。

**否决不是永久判决，是当前阶段的成本收益判断。** 所以每条否决都绑定了一个具体的
演进触发信号，而不是一句「以后再说」。
